## Codebase Patterns

### File Conventions
- Mock data files: `/app/data/mock-*.ts` with MOCK_* exports, typed imports from `~/services/types`
- Service files: `/app/services/*.ts` with async functions wrapping mock data
- UI components: `/app/components/ui/*.tsx` — 34 existing ShadCN components
- Chat components: `/app/components/chat/*.tsx`
- Stores: `/app/stores/*-store.ts` using Zustand

### Styling Patterns
- Beveled 3D borders: border-t-taupe-2 border-l-taupe-2 border-b-taupe-3 border-r-taupe-3
- Mono labels: font-[family-name:var(--mono)] or font-mono
- Small labels: text-[0.625rem] or text-[0.6875rem]
- File panel tab active: text-berry-3 !bg-off-white with transparent bottom border
- Hover states: hover:bg-berry-1 hover:text-berry-5 (light), dark:hover:bg-[rgba(var(--violet-3-rgb),0.12)]
- Focus: focus-visible:outline-2 focus-visible:outline-violet-3 focus-visible:outline-offset-2
- Shadows: shadow-[3px_3px_0_rgba(0,0,0,0.15)] dark:shadow-[3px_3px_0_rgba(0,0,0,0.3)]

### Component Patterns
- All components use data-slot attributes
- Props typed with interfaces, JSDoc on exports
- Sub-components (SpreadsheetView, FolderView) defined in same file as parent
- Conditional rendering with cn() from ~/lib/utils
- Switch component from ~/components/ui/switch for toggles
- Dialog from ~/components/ui/dialog for modals
- Skeleton from ~/components/ui/skeleton for loading states

### Existing Key Components to Reuse
- AttachButton: ~/components/ui/attach-button.tsx — already has 'computer' | 'drive' types
- FilePanel: ~/components/chat/file-panel.tsx — has 'spreadsheet' | 'folder' tabs
- ChatInput: ~/components/chat/chat-input.tsx — has staged files strip, onAttach prop
- Switch: ~/components/ui/switch.tsx — toggle component with size variants
- Dialog: ~/components/ui/dialog.tsx — modal dialog
- Skeleton: ~/components/ui/skeleton.tsx — loading placeholder

---

## Iteration Log

---
[Iteration 1] Story US-001: Cloud storage TypeScript types
Date: 2026-03-18
Summary: Added CLOUD STORAGE section to /app/services/types.ts with all 9 interfaces (CloudProvider, SharePointSite, SharePointLibrary, GoogleDriveConfig, GoogleSharedFolder, CloudStorageSettings, CloudFile, CloudSource, DataScopeToggle). Matched existing section header style (// ==== blocks) and JSDoc comment patterns.
Files modified:
- /app/services/types.ts
Learnings:
- types.ts uses /** JSDoc */ on each interface and each field
- Section headers use // ==== with ALL CAPS section names
- Existing pattern: type unions for status/category fields, optional fields with ?

---
[Iteration 2] Story US-002: Cloud storage mock data
Date: 2026-03-18
Summary: Created /app/data/mock-cloud-storage.ts with all 8 mock data exports following existing conventions (MOCK_* naming, typed imports from ~/services/types, // ==== section headers). Includes providers, SharePoint sites, Google Drive config, combined settings, cloud source tree, cloud files keyed by source ID, recent cloud files, and data scope toggles.
Files modified:
- /app/data/mock-cloud-storage.ts
Learnings:
- Mock data files use ALLCAPS const exports with MOCK_ prefix
- Record<string, CloudFile[]> pattern works well for files keyed by source/folder ID
- Recent files are a flat array referencing the same file shapes from the keyed collections

---
[Iteration 3] Story US-003: Cloud storage service layer
Date: 2026-03-18
Summary: Created /app/services/cloud-storage.ts with 6 async functions wrapping mock data. Follows exact same pattern as panels.ts and threads.ts — file header comment block, typed imports, JSDoc on each function, spread copies for array returns.
Files modified:
- /app/services/cloud-storage.ts
Learnings:
- Service files spread-copy arrays ([...MOCK_*]) to prevent mutation of mock data
- searchCloudFiles uses Object.values().flat().filter() to search across all keyed file entries
- Pattern: file header uses // ==== blocks, then import mock data, import types separately

---
[Iteration 4] Story US-004: Expand chat store for cloud drive state
Date: 2026-03-18
Summary: Added cloud drive state fields and actions to chat-store.ts. Expanded FilePanelTab to include 'cloud'. Added 6 state fields (cloudDriveMode, selectedCloudFiles, cloudActiveSourceId, cloudBreadcrumb, cloudSearchQuery, cloudSearchActive) and 7 actions (openCloudDrive, setCloudActiveSource, setCloudBreadcrumb, toggleCloudFileSelection, clearCloudSelection, setCloudSearchQuery, setCloudSearchActive).
Files modified:
- /app/stores/chat-store.ts
Learnings:
- Store uses set() with object spread for simple updates, set((state) => ...) for state-dependent updates (toggleCloudFileSelection)
- openCloudDrive clears search state and selection to ensure clean tab switch
- No existing code was modified — all additions only
